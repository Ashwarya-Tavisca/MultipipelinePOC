pipeline {
    agent any

    stages {
        stage('Build') {
            when {
                expression { check () }
            }
            steps {
                echo "Hello"
            }
        } 
              
    }
}
def version() {
 return true;
}